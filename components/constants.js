
const modelAssets = {
  // 'assets/path/to/model/file': require('./assets/path/to/model/file')  //key must have no preceding slash

}

const uri = ''

const baseUrl = 'http://localhost'

export { modelAssets, uri, baseUrl }
